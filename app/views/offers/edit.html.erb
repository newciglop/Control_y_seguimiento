<%  if current_user.role_id == User.ADMINISTRATOR || @offer.user_id == current_user.id  || current_user.role_id == User.EDITOR %>

<div class="row">
  <div class="col-sm-10 offset-sm-1 text-center">
    <h3 class="my-4">Editar licitación</h3>
  </div>
</div>

  <script type="text/javascript" charset="utf-8">

  </script>
<%= render 'form', offer: @offer %>
  <% else %>
 <%= render :partial => "layouts/views_partials/not_found"%>
<% end %>


